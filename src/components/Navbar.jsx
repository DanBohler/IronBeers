import React from 'react';
import { Link } from 'react-router-dom';

const navbar = () => {
    return(
        <nav>
            <header>
                <Link to='/'></Link>
            </header>
        </nav>
    );
}

export default navbar;